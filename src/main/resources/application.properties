# ?? ?? ??
server.port=8080

# [??] GPT API ?? (?? ??? ??)
gpt.api.key=${GPT_KEY}

# [??] TourAPI 4.0 ??
tourapi.baseUrl=http://apis.data.go.kr/B551011/KorService2
tourapi.serviceKey=${TOUR_KEY}

# [??] Kakao REST API ??
kakao.rest.api.key=${KAKAO_KEY}

# [??] T-map ?? API ??
tmap.api.key=${TMAP_KEY:}

# [??] ODSAY ?? API ??
odsay.api.key=${ODSAY_KEY:}

# HTTP ?? ?? ?? ??? ??
server.max-http-request-header-size=65536

# MySQL / MariaDB ?? ??
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/travel?useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# JPA ?? (????? SQL ???? ??? ????? ??? ???? ??)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ???????? MySQL ??(Dialect)? ??? ??
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect